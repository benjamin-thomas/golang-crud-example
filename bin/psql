#!/bin/bash

docker run --rm -it --name "$PGDATABASE-psql-interactive" \
         --env PGDATABASE=$PGDATABASE \
         --env PGHOST=$PGHOST \
         --env PGPORT=$PGPORT \
         --env PGUSER=$PGUSER \
         --env PGPASSWORD=$PGPASSWORD \
         postgres psql "$@"
